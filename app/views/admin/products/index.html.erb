<h4>ヘッダー（仮）</h4>
<%= link_to "商品新規登録ページリンク",new_admin_product_path %>
<br></br>

（管理者）商品一覧

<br>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
      <th>ジャンル</th>
      <th>ステータス</th>
    </tr>
  </thead>
  <tbody>
  <% @products.each do |product| %>
    <tr>
      <td><%=link_to product.id %></td>
      <td><%=link_to product.name,admin_product_path(product.id) %></td>
      <td><%= product.price %></td>
      <td>ジャンル工事中<%#= product.genre.name %></td>
      <td>
        <% if product.is_active == true %>
          販売中
        <% else %>
          販売停止中
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>